<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 挂载全局音频效果器 / 修改音频设置 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 前台服务（保持监听播放状态） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 13+ 通知 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 11+ 包可见性：列出全部应用（侧装应用可用；上架 Play 需申报） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- 声明后才会出现在「设置 → 使用情况访问」列表中，供用户手动授权 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- exported=true：允许用 adb 命令启停，便于自动化与调试 -->
        <service
            android:name=".BoostService"
            android:exported="true" />

        <!-- 用于获得 MediaSessionManager.getActiveSessions() 资格（识别正在播放的应用） -->
        <service
            android:name=".NotiListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- adb / 自动化设置增益的入口 -->
        <receiver
            android:name=".GainReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.rk.appvolume.SET_GAIN" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
